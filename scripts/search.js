const searchApps = () => {
    let filter = document.getElementById('Search').value.toUpperCase();
    let card = document.getElementByClassName('card');
    let titel = card.getElementByClassName('text-title');
    console.log('titel');
}